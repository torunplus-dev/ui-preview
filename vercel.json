{
  "framework": "vite",
  "buildCommand": "pnpm build",
  "installCommand": "corepack enable && corepack prepare pnpm@10.18.3 --activate && node -v && pnpm -v && pnpm install --frozen-lockfile",
  "outputDirectory": "dist"
}
